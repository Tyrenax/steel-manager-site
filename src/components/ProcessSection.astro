---
const solutions = [
  { icon: '/images/13.png', label: 'Production' },
  { icon: '/images/14.png', label: 'Optimisation' },
  { icon: '/images/15.png', label: 'CNC' },
  { icon: '/images/16.png', label: 'Stocks' },
  { icon: '/images/17.png', label: 'Achats' },
  { icon: '/images/18.png', label: 'IA' },
  { icon: '/images/19.png', label: 'Planning' },
  { icon: '/images/20.png', label: 'Livraison' },
  { icon: '/images/21.png', label: '3D' },
  { icon: '/images/22.png', label: 'Temps' },
  { icon: '/images/23.png', label: 'Qualité' },
  { icon: '/images/24.png', label: 'Finance' },
];
---

<section id="processus" class="process-section process-section--gradient section-anchor-line section-spacing">
  <div class="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
    <div class="text-center mb-9" data-aos="fade-up">
      <span class="text-primary text-sm font-semibold tracking-wider uppercase">
        Cas d'usage
      </span>
      <h2 class="text-3xl sm:text-4xl font-bold text-dark mt-2 mb-3">
        Couvrez tous vos processus
      </h2>
      <p class="text-text-light text-lg max-w-2xl mx-auto">
        De l'atelier au pilotage global, vos flux restent connectés et lisibles.
      </p>
    </div>

    <div class="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-3.5 sm:gap-4 lg:gap-5 mb-9" data-aos="fade-up" data-aos-delay="90">
      {solutions.map((solution) => (
        <div class="process-item">
          <img
            src={solution.icon}
            alt={solution.label}
            style="width: 64px; height: 64px; object-fit: contain;"
            class="process-item__icon"
          />
          <span class="process-item__label">
            {solution.label}
          </span>
        </div>
      ))}
    </div>

    <div class="text-center" data-aos="fade-up" data-aos-delay="150">
      <a href="#contact" class="process-cta">
        <span class="process-cta__dot"></span>
        <span class="process-cta__text">Tout autre processus industriel spécifique</span>
        <svg class="process-cta__arrow" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M13.5 4.5L21 12m0 0l-7.5 7.5M21 12H3" />
        </svg>
      </a>
    </div>
  </div>
</section>

<style>
  .process-section {
    position: relative;
    overflow: hidden;
  }

  .process-section--gradient {
    background:
      radial-gradient(ellipse 82% 62% at 20% 100%, rgba(6, 214, 160, 0.06) 0%, transparent 54%),
      radial-gradient(ellipse 62% 52% at 82% 0%, rgba(67, 97, 238, 0.07) 0%, transparent 52%),
      linear-gradient(180deg, #f1f3f9 0%, #f8f9fc 56%, #ffffff 100%);
  }

  .process-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
    border-radius: 14px;
    padding: 0.95rem 0.65rem;
    border: 1px solid rgba(67, 97, 238, 0.12);
    background: rgba(255, 255, 255, 0.74);
    min-height: 132px;
    transition: border-color 0.2s ease, box-shadow 0.2s ease, background-color 0.2s ease;
  }

  .process-item:hover {
    border-color: rgba(67, 97, 238, 0.28);
    background: rgba(255, 255, 255, 0.92);
    box-shadow: 0 12px 20px -18px rgba(26, 29, 41, 0.6);
  }

  .process-item__icon {
    filter: saturate(1.02);
  }

  .process-item__label {
    color: var(--color-dark);
    font-size: 1.03rem;
    font-weight: 600;
    margin-top: 0.45rem;
    line-height: 1.2;
  }

  .process-cta {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.68rem 1.25rem;
    font-size: 0.96rem;
    font-weight: 500;
    color: var(--color-accent-dark);
    background: rgba(6, 214, 160, 0.08);
    border: 1px solid rgba(6, 214, 160, 0.2);
    border-radius: 999px;
    text-decoration: none;
    transition: all 0.25s ease;
  }

  .process-cta:hover {
    background: rgba(6, 214, 160, 0.13);
    border-color: rgba(6, 214, 160, 0.32);
  }

  .process-cta__text {
    white-space: nowrap;
  }

  .process-cta__dot {
    width: 7px;
    height: 7px;
    border-radius: 50%;
    background: var(--color-accent);
    flex-shrink: 0;
  }

  .process-cta__arrow {
    flex-shrink: 0;
    opacity: 0.7;
    transition: opacity 0.2s ease, transform 0.2s ease;
  }

  .process-cta:hover .process-cta__arrow {
    opacity: 1;
    transform: translateX(2px);
  }

  @media (max-width: 520px) {
    .process-item {
      min-height: 120px;
      padding: 0.85rem 0.45rem;
    }

    .process-item__label {
      font-size: 0.98rem;
    }

    .process-cta {
      font-size: 0.84rem;
      padding: 0.62rem 0.95rem;
      gap: 0.42rem;
    }
  }
</style>
